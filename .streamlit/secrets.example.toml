# Copy values into Streamlit Community Cloud's Secrets UI or a local
# .streamlit/secrets.toml file. Do not commit real keys.
EDINET_API_KEY = ""
DATABASE_URL = "sqlite:///data/app.sqlite"
ENV = "local"
